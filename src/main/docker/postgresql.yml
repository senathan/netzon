version: '2'
services:
  netzon-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Netzon/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Netzon
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
